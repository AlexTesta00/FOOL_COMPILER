let
  var y:int = 2;
  var x:int = 4;
  var z:int = 10;
  var exp1:bool = (x <= y);
  var exp2:bool = (x >= y);
in
  print (
    if ((exp1 || exp2) && exp2)
      then { (z - x) }
      else { (z / 2) }
  );